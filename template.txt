const Client = require('../Client/Client') /** Just a Template if i want to make a new command */
const Discord = require('discord.js')
const signatureblue = '0070FF'

module.exports = {
  name: '',
  run: async(client, message, args) => {
    
  },
  aliases: [''],
  description: '',
  usage: ``
}